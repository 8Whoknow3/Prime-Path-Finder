# Prime Path Analysis & CFG Extraction in Python

این پروژه مجموعه‌ای از ابزارها برای **تحلیل مسیرهای اجرایی (Prime Paths)** و **استخراج گراف جریان کنترل (CFG)** از کدهای پایتون است.  
هدف اصلی آن کمک به تحلیل پوشش تست، تست مبتنی بر گراف و درک ساختار کنترلی برنامه‌ها می‌باشد.

---

## ✨ ویژگی‌ها

- استخراج **تمام مسیرهای ساده (Simple Paths)** از یک گراف
- شناسایی **Prime Paths** مطابق تعریف تست مبتنی بر گراف
- پشتیبانی از گراف‌های دارای **چرخه**
- استخراج یک **CFG ساده‌شده** از سورس کد پایتون با استفاده از `ast`
- نمایش خوانای adjacency list
- مثال‌ها و تست‌های آماده

---

## 📌what is prime path?

**Prime Path** مسیری ساده در گراف است که:
- قابل گسترش به مسیر ساده‌ی طولانی‌تر نباشد
- یا به‌صورت زیردنباله‌ی پیوسته در هیچ مسیر ساده‌ی دیگری وجود نداشته باشد

این مفهوم در **Graph-Based Software Testing** برای دستیابی به پوشش قوی مسیرها استفاده می‌شود.

---

## 📂 ساختار پروژه

```

.
├── prime_path_finder.py  # کد اصلی پروژه
└── README.md             # مستندات

````

---

## 🚀 نحوه استفاده

### 1️⃣ محاسبه Prime Paths از یک گراف دلخواه

```python
from prime_path_finder import prime_paths_from_adj

G = {
    'A': ['B', 'C'],
    'B': ['C', 'D'],
    'C': ['D'],
    'D': []
}

prime_paths = prime_paths_from_adj(G)
for p in prime_paths:
    print(p)
````

---

### 2️⃣ استخراج CFG از کد پایتون

```python
from prime_path_finder import cfg_from_python_source, prime_paths_from_adj

source_code = """
def f(x):
    if x > 0:
        y = 1
    else:
        y = -1
    return y
"""

cfg = cfg_from_python_source(source_code)
prime_paths = prime_paths_from_adj(cfg)
```

---

### 3️⃣ نمایش گراف CFG

```python
from prime_path_finder import print_adjacency_list

print_adjacency_list(cfg)
```

خروجی به‌صورت adjacency list نمایش داده می‌شود که برای تحلیل یا پردازش بیشتر مناسب است.

---

## 🧠 پشتیبانی‌شده در CFG

استخراج CFG فعلی از ساختارهای زیر پشتیبانی می‌کند:

* `if / else`
* `for`
* `while`
* `return`
* assignment و expressionها

> توجه: این یک **CFG ساده‌شده** است و برای تحلیل‌های آموزشی و تست مناسب می‌باشد، نه تحلیل‌های کامپایلری سطح پایین.

---

## 🧪 تست‌های موجود

در فایل اصلی، مثال‌هایی برای موارد زیر وجود دارد:

* گراف ساده بدون چرخه
* گراف دارای چرخه
* استخراج CFG از تابع ساده
* استخراج CFG از کد با چند شرط پشت‌سرهم

تمام تست‌ها از طریق اجرای مستقیم فایل قابل مشاهده هستند:

```bash
python prime_paths.py
```

---

## 📦 پیش‌نیازها

* Python 3.9+
* هیچ کتابخانه‌ی خارجی‌ای نیاز نیست (فقط `ast` استاندارد)

---

## 📚 کاربردها

* تست مبتنی بر گراف (Graph-Based Testing)
* تحلیل پوشش مسیر (Path Coverage)
* آموزش مهندسی نرم‌افزار و تست
* تحلیل ساختار کنترلی برنامه‌ها
* ابزار کمکی برای تست واحد و تست سیستم

